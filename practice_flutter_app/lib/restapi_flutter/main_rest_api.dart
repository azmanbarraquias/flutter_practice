import 'package:flutter/material.dart';

void main() => runApp(const RestAPISample());

class RestAPISample extends StatelessWidget {
  const RestAPISample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
